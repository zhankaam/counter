/*
import React, {ChangeEvent} from "react";
import "../App.css"
type DisplayPropsType = {
 count: number
 incrementFunction: () => void
 disabledButton: (count: number) => void
 maximumValue: number
 setMaximumValue: (count: number) => void
 startValue: number
 setStartValue:(count: number) => void
 activeMaxValue: boolean
 setActiveMaxValue:(count: boolean) => void
 activeMinValue: boolean
 setActiveMinValue: (count: boolean) => void
 warning: string
 setWarning: (warning: string) => void
}

export let Display: React.FC<DisplayPropsType> = (props:DisplayPropsType) => {
 (props.startValue === 0 && props.maximumValue > 0 ) || (props.maximumValue > 0
     && props.maximumValue > props.startValue  && props.startValue >= 0 )
 ? props.setWarning(`enter value and press 'set'`)
     : props.setWarning('Incorrect value!')


 }














}*/
